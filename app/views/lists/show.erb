<h2><%= @list.title %></h2>
<ul>
    <% @list.items.each do |item| %>
        <li>
            <%= item.name %> (Qty: <%= item.quantity %>)
            <form action=<%= "/items/#{item.id}/edit" %> method="GET" style="display: inline">
                <input type="submit" value="Edit">
            </form>
            <form action=<%= "/items/#{item.id}" %> method="POST" style="display: inline">
                <input type="hidden" name="_method" value="DELETE">
                <input type="submit" value="Delete">
            </form>
        </li>
    <% end %>
</ul>
<br>

<form action="/items" method="POST">
    <label for="name">Item Name: </label>
    <input type="text" name="item[name]">
    <label for="quantity">Quantity: </label>
    <input type="number" name="item[quantity]" min="1" style="width: 3em">
    <input type="submit" value="Add Item">
</form>
<br>
<a href=<%= "/users/#{current_user.id}" %>><< Back</a>